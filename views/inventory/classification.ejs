<%# Display content heading, a message, and the inventory items display. %>

<%# Content heading %>
<% if (title) { %>
<h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>
<%# A message %>

<%# messages() %>

<%# The inventory items display %>
<%- grid %>